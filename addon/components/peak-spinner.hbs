{{yield (component "peak-spinner-button" click=(fn this.onDecrement))
        (component "peak-spinner-input" change=(fn (perform this.onChange)) ref=(fn this.setRef))
        (component "peak-spinner-button" click=(fn this.onIncrement))}}
